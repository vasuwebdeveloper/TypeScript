console.log("watch.ts");

/**
 * Watch Mode
 * SIngle file (tsc app.ts --watch)
 *  Working with multiple files watch mode ( tsc -- init(which creates the ts.config file to manage the watch mode for all the files))
 * Then click on tsc to compile the project folder
 *  For watch version of project folder (tsc --watch)
*/


